import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      colors: {
        primary: "#D58F16",
        secondary: "#F1B643",
        accent: "#CDC58E",
        muted: "#BDBBBC",
      },
    },
  },
  plugins: [],
};

export default config;